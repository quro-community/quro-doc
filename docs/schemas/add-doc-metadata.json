{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "thread_id": {
      "type": "string",
      "description": "可选的弱分组ID（替代 question_id）"
    },

    "importance": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "重要性（用于排序/压缩）"
    },

    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "内容可信度（人或LLM估计）"
    }
  }
}